#ifndef main
#define main

#include <stdio.h>
#include <unistd.h>

/**
 *
 *
 *
 *
 */
typedef struct format
{
  char *vars;
  int (*f)(va_list);
}



int _putchar(char c);
int _printf(const char *format, ...);

#endif
